# Azure DevOps Build Pipeline for test-CI container
# Trying to prevent all the docker labels ADO likes to inject into the images
# DEBUG: Not sure wth its doing with vars atm....

name: $(BuildDefinitionName)-$(SourceBranchName)-$(Date:yyyyMMdd)$(Rev:.r)

variables:
  system.debug: false
  CI_PLATFORM: azure-pipelines
  IMAGE_NAME: jsloan117/test-ci
  ADO_BRANCH: $(Build.SourceBranchName)

schedules:
  - cron: '0 0 * * 6'
    displayName: Weekly build at midnight
    branches:
      include:
        - '*'
    always: true

trigger:
  branches:
    include:
      - '*'
  tags:
    include:
      - '*'

pr:
  branches:
    include:
      - '*'

pool:
  vmImage: ubuntu-18.04

steps:
  - task: Docker@2
    displayName: Docker login
    inputs:
      containerRegistry: 'Docker Hub'
      command: login
    enabled: false

  - task: Bash@3
    displayName: Install latest version of docker
    condition: succeeded()
    inputs:
      targetType: inline
      script: |
        sudo apt-get update
        sudo apt-get -y install apt-transport-https ca-certificates curl software-properties-common
        curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
        sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
        sudo apt-get update && sudo apt-get -y install docker-ce docker-ce-cli containerd.io
        sudo systemctl enable --now docker.service
    enabled: true

  - task: Bash@3
    displayName: Get environment info
    condition: succeeded()
    inputs:
      targetType: inline
      script: |
        env
        docker --version
        printf "\n\n\n"
        docker info
    enabled: false

  - task: Bash@3
    displayName: Set image tag
    condition: succeeded()
    inputs:
      targetType: inline
      script: |
        printf '\n<<< Setting image tag >>>\n\n'
        if [ "$ADO_BRANCH" == "master" ]; then IMAGE_TAG=latest; else IMAGE_TAG=$ADO_BRANCH; fi
        printf "\nImage tag is $IMAGE_TAG\n"
        echo "##vso[task.setvariable variable=IMAGE_TAG]$IMAGE_TAG"
    enabled: true

  - task: Bash@3
    displayName: Build docker image
    condition: succeeded()
    inputs:
      targetType: inline
      script: |
        printf '\n<<< Building image >>>\n\n'
        sudo docker build -f Dockerfile -t "$IMAGE_NAME:$CI_PLATFORM-$IMAGE_TAG" .
    enabled: true

  - task: Bash@3
    displayName: Build ubuntu docker image
    condition: succeeded()
    inputs:
      targetType: inline
      script: |
        printf '\n<<< Building ubuntu image >>>\n\n'
        sudo docker build -f Dockerfile.ubuntu -t "$IMAGE_NAME:ubuntu-$CI_PLATFORM-$IMAGE_TAG" .
    enabled: true

  - task: Bash@3
    displayName: Test images
    condition: succeeded()
    inputs:
      targetType: inline
      script: |
        docker run --rm $IMAGE_NAME:$CI_PLATFORM-$IMAGE_TAG bash --version
        docker run --rm $IMAGE_NAME:ubuntu-$CI_PLATFORM-$IMAGE_TAG bash --version
    enabled: true

  - task: Bash@3
    displayName: Inspect image labels
    condition: succeeded()
    inputs:
      targetType: inline
      script: |
        printf '\n<<< Inspecting images >>>\n\n'
        for img in $(sudo docker images | awk '{OFS = ":";} {print $1,$2}' | tail -n+2 | head -n2); do 
          sudo docker image inspect -f '{{json .Config.Labels}}' "${img}"; printf '\n'; 
        done
    enabled: true

  - task: Bash@3
    displayName: Build documentation
    condition: succeeded()
    inputs:
      targetType: inline
      script: |
        printf '\n<<< Building documentation >>>\n\n'
        python --version
        python3 --version
        pip --version
        pip3 --version
        sudo -H pip install mkdocs mkdocs-material
        mkdocs build -cs
        #mkdocs gh-deploy -m "Deployed {sha} with MkDocs version {version}. [skip ci]" --force
        #sudo docker run --rm -v $(pwd):/docs jsloan117/docker-mkdocs mkdocs gh-deploy -m "Deployed {sha} with MkDocs version {version}. [skip ci]" --force
    enabled: true

  - task: Bash@3
    displayName: Push images
    #if [ "$TRAVIS_PULL_REQUEST" = "false" ] && [ "$TRAVIS_BRANCH" = "dev" -o "$TRAVIS_BRANCH" = "master" ]; then
    #condition: and(succeeded(), eq(variables['Build.Reason'], 'PullRequest'))
    condition: and(succeeded(), ne(variables['Build.Reason'], 'PullRequest'), and(or(eq(variables['Source.BranchName'], 'master'), eq(variables['Source.BranchName'], 'dev'))))
    inputs:
      targetType: inline
      script: |
        printf '\n<<< Push images if not PR & Branch master or dev. >>>\n\n'
    enabled: true

  - task: Docker@2
    displayName: Docker logout
    inputs:
      containerRegistry: 'Docker Hub'
      command: logout
    enabled: false
